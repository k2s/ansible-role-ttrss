base with ntp
grab timezone from host
base with apt-get update + upgrade?
backups!
restores!
figure out something good for password management
all nginx configs use separate error logs?
SMTP settings

